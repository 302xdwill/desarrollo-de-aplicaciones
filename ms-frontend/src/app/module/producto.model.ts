export class Producto {
  id: number;
  codigo: string;
  nombre: string;
  descripcion: string;
  precio: number;
  fechaCreacion: string;  // Asegúrate de que este tipo sea compatible con la fecha que devuelve tu backend
}
